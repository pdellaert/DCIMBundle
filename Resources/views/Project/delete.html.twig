{% extends "DellaertDCIMBundle::layout.html.twig" %}

{% block content %}
	{% if entity is defined %}
    	<script type="text/javascript">
    	setTimeout(function() {
    		  window.location.href = "{{ app.session.get('return_url') }}";
    		}, 3000);
    	</script>
    	<h1>Project deleted: {{ entity.title }}</h1>
    	<p>You will be returned to the project list in 3 seconds...</p>
    {% else %}
		<h1>Unknown project</h1>
		<p>The specified project is not available.</p>
    {% endif %}
{% endblock %}
